#include <stdio.h>

int main() {
    int x, y, max = 0;
    printf("운동장 3개의 가로, 세로 길이를 차례로 입력하세요;\n");

    for (int i = 0; i < 3; i++) {
        scanf("%d %d", &x, &y);
        if (x * y > max)
            max = x * y;
    }

    printf("가장 큰 운동장의 넓이는 %d 입니다.\n", max);
    return 0;
}
