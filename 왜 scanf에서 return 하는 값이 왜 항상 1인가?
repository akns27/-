#include <stdio.h>

int main() {
    char str[100];
    int a, b, i, j, n;
    a = scanf("%c", str);
    b= printf("a=%d str=%s", a, str);
    printf("b=%d",&b);
  return 0;
}
//이 코드에서 scanf에서 return 하는 값이 왜 항상 1인가?
//scanf() 함수는 형식 문자열에 지정된 형식에 따라 입력에서 데이터를 읽습니다. 입력에서 데이터를 성공적으로 읽을 때마다 scanf() 함수는 읽은 데이터의 수와 동일한 정수를 반환합니다. 입력에서 데이터를 읽을 수 없으면 scanf() 함수는 EOF(End of File)를 반환합니다.

//scanf() 함수는 반환 값이 1이면 항상 입력에서 데이터를 성공적으로 읽었음을 의미합니다. 이는 scanf() 함수가 항상 입력에서 최소한 하나의 문자를 읽으려고 시도하기 때문입니다. scanf() 함수가 입력에서 최소한 하나의 문자를 읽을 수 없으면 EOF를 반환합니다.

//그러나 귀하의 코드에서 입력에서 문자를 읽으려고 시도하지만 입력은 비어 있습니다. 따라서 scanf() 함수는 항상 최소한 하나의 문자를 읽을 수 있고 반환 값은 1입니다.
