#include<stdio.h>
int f(int a, int b, int c, int d)
{
    return a+b+c+d;
}
main()
{
    int a, b, c, d;
    scanf("%d %d %d %d, &a, &b, &c, &d");
    if (a>1 or a<0 || b>1 or b<0 || c>1 or c<0 || d>1 or d<0)
    {
        printf("입력 오류");
        return 0;
    }
    switch(f(a, b, c, d))
    {
        case 1 : printf("도\n"); break;
        case 2 : printf("개\n"); break;
        case 3 : printf("걸\n"); break;
        case 4 : printf("윷\n"); break;
        default : printf("모"); 
        
    }
    return 0;
}
