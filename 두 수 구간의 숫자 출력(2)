#include <stdio.h>

int main() {
    int start, end;

    printf("시작 숫자를 입력하세요: ");
    scanf("%d", &start);

    printf("끝 숫자를 입력하세요: ");
    scanf("%d", &end);

    printf("%d부터 %d까지의 숫자:\n", start, end);

    for (int i = start; i <= end; i++) {
        printf("%d\n", i);
    }

    return 0;
}
