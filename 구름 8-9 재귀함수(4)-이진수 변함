#include <stdio.h>
 void f(int n){
 	if (n==0) return; //몫이 0이면 종료
    f(n/2); //n을 2로 나눈 몪을 값
    printf("%d",n % 2);  //이진수 한자리씩 출력     
}

int main()
{   
    int n;
    scanf( "%d", &n );
    f(n);  
    return 0;
}
//f() 함수는 숫자를 이진수로 변환하는 함수입니다. 함수는 숫자를 2로 나눈 몫을 재귀적으로 호출하여 숫자를 줄여갑니다. 몫이 0이 되면 함수는 종료됩니다. 함수는 몫이 0이 아닐 때는 숫자를 2로 나눈 나머지를 출력합니다.

//이 코드에서 n % 2를 사용하는 이유는 숫자를 2로 나눈 나머지를 구하기 위해서입니다. 숫자를 2로 나눈 나머지는 숫자의 이진수 표현에서 가장 오른쪽에 있는 숫자입니다. 따라서 n % 2를 사용하여 숫자의 이진수 표현을 한 자리씩 출력할 수 있습니다.
